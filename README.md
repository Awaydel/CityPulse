# EcoSense Analytics

Проект для аналитики экологических данных, предоставляющий дашборд с визуализацией измерений по городам.

## 🛠 Технологический стек

### Frontend
- **React 19** (Vite)
- **TypeScript**
- **Recharts** (для графиков)
- **Lucide React** (для иконок)

### Backend
- **Python 3.x**
- **FastAPI**
- **Uvicorn**
- **Psycopg2** (PostgreSQL driver)
- **Pandas** & **Scikit-learn** (для обработки данных)

### Database
- **PostgreSQL**

## 📋 Предварительные требования

Перед началом работы убедитесь, что у вас установлены:
- [Node.js](https://nodejs.org/) (версия 18+ рекомендуется)
- [Python](https://www.python.org/) (версия 3.9+)
- [PostgreSQL](https://www.postgresql.org/)

## 🚀 Установка и запуск

### 1. Настройка базы данных

1. Убедитесь, что сервер PostgreSQL запущен.
2. Создайте базу данных с именем `ecosense` (или используйте настройки из `server.py`).
3. Выполните SQL скрипты для создания структуры таблиц. Вы можете использовать `ecosenseDB.sql` или `ecosense.sql` (проверьте актуальность).

```bash
psql -U postgres -d ecosense -f ecosenseDB.sql
```

> **Примечание:** По умолчанию в `server.py` используются следующие настройки подключения:
> - Host: `127.0.0.1`
> - Port: `5432`
> - User: `postgres`
> - Password: `123`
> - DB: `ecosense`
>
> Если ваши настройки отличаются, измените переменную `DB_CONFIG` в файле `server.py`.

### 2. Настройка Backend

1. Создайте виртуальное окружение (рекомендуется):
   ```bash
   python -m venv venv
   # Windows
   venv\Scripts\activate
   # Linux/macOS
   source venv/bin/activate
   ```

2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

3. Заполните базу данных начальными данными (ETL):
   ```bash
   python services/etl.py
   ```

4. Запустите сервер API:
   ```bash
   python server.py
   ```
   Сервер будет доступен по адресу: `http://localhost:8000`
   Документация API (Swagger): `http://localhost:8000/docs`

### 3. Настройка Frontend

1. Установите зависимости:
   ```bash
   npm install
   ```

2. Запустите режим разработки:
   ```bash
   npm run dev
   ```
   Приложение будет доступно по адресу, указанному в консоли (обычно `http://localhost:5173`).

## 📂 Структура проекта

- `server.py` - Основной файл запуска Backend API.
- `App.tsx` - Основной компонент React приложения.
- `services/` - Сервисные скрипты (например, `etl.py` для загрузки данных).
- `components/` - Компоненты React.
- `ecosenseDB.sql` / `ecosense.sql` - Дампы/схемы базы данных.
- `requirements.txt` - Зависимости Python.
- `package.json` - Зависимости Node.js.

## 🔧 Возможные проблемы

- **Ошибка подключения к БД**: Проверьте, запущен ли PostgreSQL и совпадают ли учетные данные в `server.py` с вашими.
- **CORS ошибки**: Убедитесь, что в `server.py` в списке `origins` добавлен адрес вашего фронтенда (по умолчанию `http://localhost:5173` уже добавлен).
