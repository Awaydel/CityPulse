# EcoSense Analytics 🌿

**EcoSense Analytics** — это комплексная система мониторинга экологических показателей (качество воздуха, погода) с возможностью прогнозирования на основе машинного обучения. Приложение агрегирует данные из внешних источников (Open-Meteo), сохраняет их в хранилище данных и предоставляет интерактивный дашборд для анализа.

![Dashboard Preview](https://via.placeholder.com/800x400?text=EcoSense+Dashboard+Preview)

## 🚀 Особенности

*   **Мониторинг в реальном времени**: Отображение PM2.5, температуры, влажности и скорости ветра.
*   **ETL Конвейер**: Автоматический сбор и очистка данных.
*   **ML Прогнозирование**: Предсказание уровня загрязнения воздуха на основе погодных факторов.
*   **Интерактивная карта рисков**: Визуализация опасных зон.
*   **Аналитика**: Графики трендов и корреляций показателей.

## 🛠 Технологический стек

### Frontend
*   **React 19** + **Vite**: Быстрый и современный UI.
*   **TypeScript**: Типизация для надежности.
*   **Tailwind CSS**: Стилизация и дизайн-система.
*   **Recharts**: Визуализация данных (графики).
*   **Lucide React**: Иконки.

### Backend & Data
*   **FastAPI**: Высокопроизводительный API на Python.
*   **PostgreSQL**: Реляционная база данных для хранения измерений.
*   **Pandas & Scikit-learn**: Обработка данных и ML модели.
*   **Psycopg2**: Драйвер для работы с БД.

---

## 📂 Структура проекта

```
air_quality_aggregator/
├── airflow/                # Конфигурации для Apache Airflow (опционально)
├── components/             # React компоненты (UI, Графики)
├── docs/                   # Документация проекта
├── services/               # Сервисные скрипты и логика
│   ├── api.ts              # Клиент API для фронтенда
│   └── etl.py              # ETL скрипт (Extract, Transform, Load)
├── sql/                    # SQL миграции и запросы
├── App.tsx                 # Основной компонент приложения
├── ecosense.sql            # Схема базы данных
├── server.py               # Сервер FastAPI
├── package.json            # Зависимости Frontend
├── requirements.txt        # Зависимости Python
└── vite.config.ts          # Конфигурация сборщика
```

---

## ⚙️ Установка и запуск

Для работы проекта вам понадобятся установленные **Node.js**, **Python 3.9+** и **PostgreSQL**.

### 1. Настройка Базы Данных

1.  Убедитесь, что PostgreSQL запущен.
2.  Создайте базу данных `ecosense` и пользователя (параметры по умолчанию в `server.py`):
    *   **DB Name**: `ecosense`
    *   **User**: `postgres`
    *   **Password**: `123`
    *   **Host**: `127.0.0.1:5432`
3.  Примените схему базы данных:
    ```bash
    psql -U postgres -d ecosense -f ecosense.sql
    ```
    *(Либо выполните содержимое `ecosense.sql` через любой SQL-клиент)*

### 2. Настройка Backend (Python)

Рекомендуется использовать виртуальное окружение.

```bash
# Создание venv (Windows)
python -m venv venv
venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt
```

### 3. Настройка Frontend (React)

```bash
# Установка node_modules
npm install
```

---

## ▶️ Запуск приложения

Вам потребуется открыть 3 терминала для полноценной работы системы.

### Терминал 1: Backend API
Запускает веб-сервер для обработки запросов от фронтенда.

```bash
python server.py
# Сервер запустится на http://localhost:8000
# Swagger документация доступна по адресу: http://localhost:8000/docs
```

### Терминал 2: Frontend
Запускает клиентскую часть приложения.

```bash
npm run dev
# Приложение доступно на http://localhost:5173
```

### Терминал 3: ETL (Загрузка данных)
Заполняет базу данных свежими данными и прогнозами. Без этого шага дашборд будет пуст.

```bash
# Запуск скрпита ETL из корня проекта
python services/etl.py
```
*Примечание: Если скрипт не находит модули, убедитесь, что вы активировали venv.*

---

## 📚 API Эндпоинты

*   `GET /api/cities` — Список доступных городов.
*   `GET /api/measurements` — Получение аналитических данных для дашборда (параметр `city_name`).

## 🧪 Обучение модели (ML)
В рамках ETL процесса (`services/etl.py`):
1.  Данные скачиваются с Open-Meteo.
2.  Обучается модель `RandomForestRegressor` (или аналог).
3.  Предсказываются значения PM2.5 на основе текущей погоды.
4.  Результаты сохраняются в таблицу `fact_air_quality`.

> 📊 Подробный анализ качества кода и отчет о тестировании доступны в файле [QA_REPORT.md](docs/QA_REPORT.md).

## 🤝 Вклад в проект
Если вы хотите доработать проект:
1.  Создайте ветку: `git checkout -b feature/AmazingFeature`
2.  Закоммитьте изменения: `git commit -m 'Add some AmazingFeature'`
3.  Запушьте ветку: `git push origin feature/AmazingFeature`
